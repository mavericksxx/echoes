# Backlog

Phases are defined in SPEC.md. Current: **Phase 1 — Village on screen with sample data** (deployed to echoes.parthkohale.com; fix pass in progress: camera, all-characters default + song cards, map watermark cleanup).

## Needs the user
- Register Spotify app (redirect `http://127.0.0.1:PORT/callback`) — Phase 2
- Gemini API key — Phase 3
- ~~Cloudflare login~~ done (wrangler already authenticated)
